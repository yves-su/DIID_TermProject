name: smart_racket
description: Smart Badminton Racket - BLE to Firebase Realtime Database

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # BLE 藍牙通訊
  flutter_blue_plus: ^1.31.0
  
  # Firebase
  firebase_core: ^2.24.2
  firebase_database: ^10.4.0  # Realtime Database
  
  # 狀態管理
  provider: ^6.1.1
  
  # 工具
  permission_handler: ^11.1.0
  uuid: ^4.2.1
  intl: ^0.18.1
  
  # 圖表
  fl_chart: ^0.66.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
